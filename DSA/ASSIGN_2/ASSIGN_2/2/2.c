#include "2.h"

int main(){
    printf("Enter the sparse matrix dimension\n");
    int row,col;
    scanf("%d%d",row,col);
    printf("how many input you have in sparse matrix\n");
    int size;
    scanf("%d",&size);
    for(int i=0;i<size;i++){
        
    }
}